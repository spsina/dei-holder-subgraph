{
  "name": "deiHolders",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ spsina/deiHolders",
    "create-local": "graph create --node http://localhost:8020/ spsina/deiHolders",
    "remove-local": "graph remove --node http://localhost:8020/ spsina/deiHolders",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 spsina/deiHolders"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
